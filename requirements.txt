# Spec Server runtime dependencies
Flask>=3.0,<4.0
flask-smorest>=0.44,<1.0
Flask-SQLAlchemy>=3.1,<4.0
marshmallow>=3.21,<4.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
gunicorn>=21.2
python-dotenv>=1.0

# Test/dev (kept here so the image can run the suite; small footprint)
pytest>=8.0
